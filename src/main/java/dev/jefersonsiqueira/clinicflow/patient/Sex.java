package dev.jefersonsiqueira.clinicflow.patient;

/** Optional on a patient record — {@code NAO_INFORMADO} is a real, deliberate answer, not a gap. */
public enum Sex {
  MASCULINO,
  FEMININO,
  OUTRO,
  NAO_INFORMADO
}
